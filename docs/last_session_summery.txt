 Here's a summary of everything done this session:

  UI Changes
  - Profile page stores now show as cards (name, category, address, "View details →") matching My Stores page
  - Replaced home page avatar/welcome header with a hamburger menu that slides in from the left with a Profile option
  - Removed the bottom tab bar
  - Added back arrow button to Profile and Add Store pages

  Add Store Page
  - Added Phone Number field (required, 10 digits validated)
  - Added multi-image upload (up to 5 images) via Appwrite Storage
  - Added Products section (optional) — each product has name*, description*, price* and optional category
  - Added red * labels on all required fields
  - Added full field validation (store name, address, phone, description)
  - Changed "Create Store" button text to "Add Store"

  Crash Prevention
  - api.ts — guards against undefined json.data from API
  - profile.tsx — safe router.back(), avatar undefined fallback, logout try/catch
  - add-store.tsx — image picker null guard, filename safety, network error handling

  Backend (stores.py)
  - Added DELETE endpoint with owner authorization check and cache invalidation
  - Added phone and images fields to CreateStorePayload
  - Fixed DocumentList SDK compatibility with to_dict / docs_list helpers
  - Fixed store delete to go through backend properly (not direct Appwrite)

  Profile Page
  - Delete store button on each store card with confirmation alert
  - Refetches store list after deletion